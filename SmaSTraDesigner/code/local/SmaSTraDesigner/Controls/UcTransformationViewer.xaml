<local:UcNodeViewer
	x:Class="SmaSTraDesigner.Controls.UcTransformationViewer"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	xmlns:local="clr-namespace:SmaSTraDesigner.Controls"
	xmlns:converters="clr-namespace:Common.Resources.Converters;assembly=PresentationCommonLib"
	mc:Ignorable="d"
	x:Name="This">
	<local:UcNodeViewer.Resources>
		<converters:LambdaConverter x:Key="InputItemsSourceConverter" />
	</local:UcNodeViewer.Resources>
	<Grid Margin="0,5,0,5">
		<Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
			<Border.Style>
				<Style TargetType="{x:Type Border}">
					<Setter Property="BorderBrush">
						<Setter.Value>
							<SolidColorBrush>
								<SolidColorBrush.Color>
									<Color R="128" G="192" B="255" A="255" />
								</SolidColorBrush.Color>
							</SolidColorBrush>
						</Setter.Value>
					</Setter>
					<Setter Property="BorderThickness" Value="2" />
					<Setter Property="Background" Value="LightBlue" />
					<Style.Triggers>
						<DataTrigger Binding="{Binding Path=Class.InputTypes.Length}" Value="1">
							<Setter Property="Background" Value="LightGray" />
							<Setter Property="BorderBrush" Value="Gray" />
						</DataTrigger>
						<DataTrigger Binding="{Binding ElementName=This, Path=IsSelected}" Value="True">
							<Setter Property="BorderBrush" Value="Black" />
							<Setter Property="BorderThickness" Value="3" />
						</DataTrigger>
						<DataTrigger Binding="{Binding ElementName=This, Path=IsInSelectionArea}" Value="True">
							<Setter Property="BorderBrush" Value="DarkBlue" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</Border.Style>
		</Border>
		<TextBlock Text="{Binding Path=Name}" FontFamily="Lucida Console" FontSize="16" FontWeight="Bold" Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20" />

		<ItemsControl x:Name="icInputHandles" ItemsSource="{Binding Path=Class.InputTypes, Converter={StaticResource InputItemsSourceConverter}}" VerticalAlignment="Top" IsHitTestVisible="True">
			<ItemsControl.Style>
				<Style TargetType="{x:Type ItemsControl}">
					<Setter Property="HorizontalAlignment" Value="Left" />
					<Setter Property="Margin" Value="5,-5,0,0" />
					<Style.Triggers>
						<DataTrigger Binding="{Binding Path=Class.InputTypes.Length}" Value="1">
							<Setter Property="HorizontalAlignment" Value="Center" />
							<Setter Property="Margin" Value="0,-5,0,0" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</ItemsControl.Style>
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<StackPanel Orientation="Horizontal" />
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<local:UcIOHandle IsInput="True" DataContext="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=DataContext.Item1}" InputIndex="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=DataContext.Item2}" Margin="5,0" />
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>

		<local:UcIOHandle IsInput="False" DataContext="{Binding ElementName=This, Path=DataContext.OutputType}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,-5" />
	</Grid>
</local:UcNodeViewer>
